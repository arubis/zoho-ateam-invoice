[project]
name = "zoho-ateam-invoice"
version = "0.1.0"
description = "Zoho Books invoice pipeline for contractor billing"
requires-python = ">=3.9"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]
